% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bypass.R
\name{with_bypass}
\alias{with_bypass}
\alias{local_bypass}
\alias{global_bypass}
\title{Bypass S3 Methods}
\usage{
with_bypass(.expr, .env = .GlobalEnv)

local_bypass(.env = .GlobalEnv, .frame = parent.frame())

global_bypass(.env = .GlobalEnv)
}
\arguments{
\item{.expr}{An expression to evaluate with temporary bindings.}

\item{.env}{An environment.}

\item{.frame}{The frame environment that determines the scope of
the temporary bindings. When that frame is popped from the call
stack, bindings are switched back to their original values.}
}
\value{
\code{local_bypass()} returns the values of old bindings invisibly;
\code{with_bindings()} returns the value of expr.
}
\description{
\itemize{
\item \code{with_bypass()} evaluates \code{expr} without triggering S3 methods for some curated
internal generics (see details)
\item \code{local_bypass()} sets up a function not to trigger these S3 methods from the
function's code
\item \code{global_bypass()} overrides internal generics to set the behavior globally
in a given environment, it's designed with packages in mind (use
\code{bypass::global_bypass(asNamespace(pkgname))} in \code{.onLoad()})
}
}
\details{
\code{with_bypass()} and \code{local_bypass()} override
\code{c}, \code{dim}, \verb{dim<-}, \code{$}, \verb{$<-}, \code{lapply}, \code{length}, \code{lengths}, \code{names},
\verb{names<-}, \code{sapply}, \code{[}, \code{[[}, \verb{[<-}, \verb{[[<-}, \code{unlist}.

These are replaced by
\code{.c}, \code{.dim}, \verb{.dim<-}, \code{.dollar}, \verb{.dollar<-}, \code{.lapply}, \code{.length},
\code{.lengths}, \code{.names}, \verb{.names<-}, \code{.sapply}, \code{.subset}, \code{.subset2}, ,
\verb{.subset<-}, \verb{.subset2<-}, \code{.unlist}
}
